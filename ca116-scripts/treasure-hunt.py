#!/usr/bin/env python3

import os
import sys

files = os.listdir(".")

i = 0
while i < len(files):
  with open("start.txt") as f
    content =  f.read().strip()
  if os.path.isfile(content) is True:
   
